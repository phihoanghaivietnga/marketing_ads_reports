Đây là dự án Marketing Analytics Dashboard. Toàn bộ requirements, system design, và task breakdown đã được chốt sẵn tại docs/01-SPEC.md — coi tài liệu này là nguồn sự thật duy nhất (single source of truth), không cần hỏi lại tao về yêu cầu nghiệp vụ.

Bỏ qua hoàn toàn bước DISCUSS. Vào thẳng bước PLAN dựa trên:
- Mục 9 (Task Breakdown) của docs/01-SPEC.md làm khung task chính (Phase A -> B -> C -> D -> E), thực hiện tuần tự, không nhảy cóc.
- Mục 5.2 (cấu trúc thư mục backend/app/...) là ràng buộc bắt buộc, không tự ý đổi cấu trúc Clean Architecture đã định nghĩa.
- database/*.sql đã có sẵn, đã đánh số thứ tự — Plan phase KHÔNG được generate lại DDL, chỉ được tham chiếu để biết tên bảng/cột khi viết  repository layer.
- backend/.env.example đã liệt kê đủ biến môi trường cần dùng — Plan  phase không tự thêm biến mới trừ khi phát sinh nhu cầu thật sự và  phải note rõ lý do.

Bắt đầu từ Phase A (Hạ tầng & Database) trong docs/01-SPEC.md mục 9.Ghi lại toàn bộ plan chi tiết (breakdown nhỏ hơn nếu cần) vào gsd-core/contexts/active-context.md trước khi bắt đầu code, để tao review plan trước khi mày chuyển sang phase EXECUTE.