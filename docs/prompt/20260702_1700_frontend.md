Đây là dự án Marketing Analytics Dashboard. Frontend đã có spec mới tại docs/02-FRONTEND-SPEC.md (v2.0) — bản này THAY THẾ hoàn toàn bố cục Overview đơn giản ở v1.0, chuyển sang trang "Platform Detail" (VD Facebook Ads Detail) theo đúng mockup đã duyệt. Coi docs/02-FRONTEND-SPEC.md là nguồn sự thật duy nhất cho phần frontend, không cần hỏi lại tao về yêu cầu nghiệp vụ.

Bỏ qua hoàn toàn bước DISCUSS. Vào thẳng bước PLAN dựa trên:
- Mục 9 (Task Breakdown, Phase K -> L -> M -> N -> O) làm khung task chính, thực hiện tuần tự.
- Mục 0 (Ghi chú quan trọng) liệt kê các dimension/metric custom (K1, K2,   Action, Getback, Doanh thu kỳ vọng...) hiện CHƯA có sẵn ở backend   (docs/01-SPEC.md). Đây là giả định tạm, không phải sự thật đã xác nhận.   Nếu backend chưa có endpoint /api/v1/platform-detail (mục 5), thực hiện   Phase L6: dùng mock data đúng shape PlatformDetailResponse (mục 4.3) để   build UI trước, đánh dấu rõ TODO ở từng chỗ dùng mock.
- Mục 4.2 (filter-fields.ts) là ràng buộc bắt buộc: 17 filter phải khai báo   dạng config-driven, KHÔNG viết tay JSX lặp lại cho từng field.
- Mục 6 (cấu trúc thư mục) và mục 7 (chi tiết component) là ràng buộc bắt   buộc, không tự ý đổi cấu trúc.
- KHÔNG tính toán nghiệp vụ ở frontend (Cost, CPM, CPA, CPK1, CPK2,   % Chi phí...) — mọi con số backend trả sẵn, frontend chỉ format hiển thị.

Bắt đầu từ Phase K (Khởi tạo & Design system) trong docs/02-FRONTEND-SPEC.md mục 9. Ghi lại plan chi tiết vào gsd-core/contexts/active-context.md trước khi bắt đầu code, để tao review trước khi chuyển sang phase EXECUTE.